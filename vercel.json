{
  "rewrites": [
    {
      "source": "/join/:code",
      "destination": "/api/join/:code"
    }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install"
}
